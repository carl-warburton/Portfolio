<%= form_for(enquiry) do |f| %>
  <% if enquiry.errors.any? %>
    <div id="error_explanation">
      <h2 ><%= pluralize(enquiry.errors.count, "error") %> prohibited this enquiry from being saved:</h2>

      <ul>
      <% enquiry.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field form-group col-lg-10">
    <%= f.label :name %>
    <%= f.text_field :name, class: "form-control" %>
  </div>

  <div class="field form-group">
    <%= f.label :email %>
    <%= f.text_field :email, class: "form-horizontal" %>
  </div>

  <div class="field form-group">
    <%= f.label :mobile %>
    <%= f.text_field :mobile, class:"form-horizontal" %>
  </div>

  <div class="field form-group">
    <%= f.label :message %>
    <%= f.text_area :message, class: "form-horizontal"%>
  </div>

  <div class="actions">
    <%= f.submit %>

  </div>
<% end %>
